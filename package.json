{
  "name": "parse-it",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "dev": "pnpm gen-manifest && concurrently \"vite\" \"wrangler pages dev public --port 8788 --compatibility-date=2024-04-03\"",
    "build": "pnpm gen-manifest && vite build",
    "preview": "vite preview",
    "start": "pnpm build && wrangler pages dev dist",
    "gen-manifest": "node scripts/generate-catalog.js",
    "deploy": "pnpm build && wrangler pages deploy dist"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^8.0.9",
    "wrangler": "^3.0.0",
    "zod": "^4.3.6"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.33.0"
}
